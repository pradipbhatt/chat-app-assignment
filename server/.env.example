# Port the API and the WebSocket handshake both listen on
PORT=5050

# Origin allowed by CORS — the Vite dev server in development, the deployed
# frontend origin in production. Must be an exact origin, no trailing slash.
CLIENT_URL=http://localhost:5173
